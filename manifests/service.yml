apiVersion: v1
kind: Service
metadata:
    name: "pmnk8s-d7d0"
    labels:
        app: "pmnk8s-d7d0"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "pmnk8s-d7d0"